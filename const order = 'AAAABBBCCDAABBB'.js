const order = 'AAAABBBCCDAABBB'
const sdf = new Set(order.split(''))
sdf
let qw = order.split('')
qw.reduce((a, b) => b - a)

qw
let gh = order.split('').filter((el, ind) => el !== order[ind - 1])
gh

let a = "loopingisfunbutdangerous"
let b = "lessdangerousthancoding"
let f = Array.from(new Set([...a].concat([...b])))

let as = f.sort()
as.join('')
as
as
f

let gf = Array.from(new Set([...a].concat([...b]).sort())).join('')


console.log(gf)
let str = "bitcoin take over the world maybe who knows perhaps"
let empty = []
let we = str.split(' ').map(el => empty.push(el.length))
empty
let g = Math.min(...empty)
g

function findShort(s){
    let arr = []
    s.split(' ').map(el => empty.push(el.length))
    return Math.min(...empty)
}
let numb = 12
let dfg = Array.from(String(numb), Number)
dfg
dfg.in
let newarr = dfg.map(el => el * Math.pow(10, (dfg.length - (dfg.indexOf(el) + 1))))
newarr
let cook = dfg.map((el, ind) => el * Math.pow(10, (dfg.length - (ind + 1)))).filter(el=>el).join(' + ')
cook


const goog = [3, 5, 5]

let doog = Array.from(String(goog.reduce((a, b) => a * b)), Number)
doog


let i = goog.length

for (let x = goog.length; goog.length <= 2; x--) {
 Array.from(String(goog.reduce((a, b) => a * b)), Number)
}

let das = [5, 4, 3, 2, 1]
let rex = das.filter((el)=> el < das.indexOf(el))
rex

let doh = [[10, 0], [3, 5], [5, 8]]


let rty = "hello"
let ert = rty.split(' ').map((el,ind)=>  )
    charAt(0).toUpperCase() + rty.slice(1)
ert
 
